# План імплементації · Концепція 3 «Тактильний Мінімалізм»

> Оглядовий план; повний покроковий — після вибору концепції.
> Спека: `../specs/2026-08-16-concept-3-tactile-minimal-design.md`.
> Найменший обсяг із трьох: нових БД-полів нема, тема не змінюється.

## Етап 0 — спільний (див. 2026-08-16-concepts-common-plan.md)

## Етап 1 · Акценти — S

- [ ] `globals.css`: `--accent` → смарагд `#1D7059` / `#4ECBA5` (пара).
- [ ] Теракоти: декоративний `#E2725B` + текстовий `#B8503B` (світла);
      підібрати і ЗАМІРЯТИ нічну текстову пару (≥4.5 на `#1b211f`).
- [ ] Розвести «два зелені» (смарагдовий лінк vs статус наявності `#16a34a`):
      рішення дизайнера — кегль/вага або єдиний смарагд для обох.
- Верифікація: перерахунок контрастів пар, прохід по сторінках.

## Етап 2 · Герой-таби з однією сценою — L (ядро концепції)

- [ ] `Home.tsx`: стек героїв → pill-таби колекцій (актив — чорна пігулка; >3 колекцій — горизонтальний скрол ряду) + ОДИН герой активної колекції.
- [ ] Перемикання колекції: своп `designs` у ТІЙ САМІЙ сцені; remount канваса
      лише при зміні власної моделі (перевірити пару Оксана ↔ Вар'ятство).
- [ ] Постер першої колекції — `priority` (LCP).
- [ ] Архівна колекція в табі — сіра мітка «архів».
- Верифікація: WebGL-контекст рівно один; таби міняють колекцію без
  перезавантаження сцени на текстурних парах.

## Етап 3 · Позначки «градієнт-або-нічого» — S (дані)

- [ ] UPDATE: всі активні `label_color` → `var(--gradient-brand)`.
- [ ] Крапки-статуси: зелена «класика», текстовий теракот «останні розміри».
- [ ] Гайд у дизайн-доці: капсули без сторонніх кольорів.
- Верифікація: /v2/audit — всі капсули градієнтні.

## Етап 4 · Комунікація нічної теми — S

- [ ] Рядок у футері «нічна тема вмикається о 18:00» (копі узгодити з
      власником — авторський тон!).
- Верифікація: рядок в обох темах читабельний.

## Порядок і залежності

1 → 2; 3 і 4 — будь-коли (3 — чисті дані, без деплою).

## Definition of done

Смарагд/теракоти в системі з AA-парами; головна = таби + одна сцена;
капсули градієнтні; нічна автоматика пояснена користувачу.
