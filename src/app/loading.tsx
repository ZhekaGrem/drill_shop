// src/app/loading.tsx
// Завантажувальний екран усього застосунку — фірмовий SiteLoader
// («Градієнтний логотип», обраний у галереї /v2/loaders).
import { SiteLoader } from '@/shared/components/SiteLoader/SiteLoader';

const Loading = () => <SiteLoader />;

export default Loading;
